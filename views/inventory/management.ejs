<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

    <%- messages() %>

    <a href="/inv/class">Add New Classification</a>
    <br>
    <a href="/inv/inv">Add New Vehicle</a>
